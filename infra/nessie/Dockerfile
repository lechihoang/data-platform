# Pin version để đảm bảo reproducibility và compatibility với
# nessie-spark-extensions-4.0_2.13:0.108.1 trong spark-defaults.conf.
# Nessie không còn publish lên Docker Hub — dùng ghcr.io.
FROM ghcr.io/projectnessie/nessie:0.108.1

# Working directory
WORKDIR /opt/nessie
